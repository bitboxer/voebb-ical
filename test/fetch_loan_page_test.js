import fs from 'fs';
import { expect } from 'chai';

import fetchLoanPage from '../src/fetch_loan_page';

describe('FetchLoanPage', () => {

  xit('should load the loan page of an user', async () => {
    // TODO: find out how to test this properly
    // const result = await fetchLoanPage(process.env.VOEBB_ID, process.env.VOEBB_PASSWORD);
  }).timeout(60000);

});
